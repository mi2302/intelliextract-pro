require('dotenv').config(); const oracledb = require('oracledb'); const dbConfig = { user: process.env.DB_USER, password: process.env.DB_PASSWORD, connectString: process.env.DB_HOST + ':' + process.env.DB_PORT + '/' + process.env.DB_SERVICE_NAME }; async function run() { let conn; try { conn = await oracledb.getConnection(dbConfig); const res = await conn.execute('SELECT * FROM XXEA_DM_TABLE_JOINS WHERE ROWNUM <= 5'); console.log('TABLE OK:', res.metaData.map(m=>m.name)); console.log(res.rows); } catch(e) { console.error('ERROR:', e.message); } finally { if(conn) await conn.close(); } }; run();
